'use strict'

window.addEventListener('load', function() {

    try {
        var year = 2019;
        alert(year);
        console.log(decodeURIComponent("https://vifjreocnjo##$$%r.es"))
    } catch (error) {
        alert(`Ocurrio el error ${error} al momento de cargar`);
    }

})